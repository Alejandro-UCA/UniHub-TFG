En este capítulo se recoge el diseño de la arquitectura del sistema informático \textbf{UniHub}, la parametrización del software base, el diseño físico de datos y el diseño detallado de la interfaz de usuario.

\section{Arquitectura del Sistema}
Se adopta una arquitectura desacoplada basada en microservicios contenerizados y un patrón en capas (Layers) orientada a la web.

\subsection{Diseño de alto nivel}
Siguiendo el modelo C4 (Contenedores y Componentes), el sistema se divide en cuatro capas principales:
\begin{enumerate}
    \item \textbf{Capa de Presentación (Frontend):} Servidor Web Nginx entregando la SPA compilada en React con soporte para la ruta aislada \texttt{/admin} (Fase 3).
    \item \textbf{Capa de Servicio (API REST):} Framework FastAPI en Python que expone controladores RESTful, métricas cgroup de contenedores, sincronización reactiva \texttt{/api/v1/admin/sync-etl} y endpoints CRUD (Fase 2).
    \item \textbf{Capa de Datos (Persistencia):} Motor de Base de Datos Relacional PostgreSQL 15 con el esquema DDL y almacenamiento de archivos JSON en disco (Fase 2 y 1).
    \item \textbf{Capa de Ingesta (Crawler):} Módulo independiente en Python organizado en cuatro partes: catálogo RUCT y resoluciones BOE, recuperación desde webs oficiales, consolidación de precios ECTS y enriquecimiento con guías docentes. La ejecución puede programarse mediante Cron y mantiene un manifiesto y puntos de control para su auditoría.
\end{enumerate}

\subsection{Diseño detallado}
La arquitectura de ingesta y servicios se rige por los siguientes componentes clave:
\begin{itemize}
    \item \textbf{Recuperación desde webs oficiales (Fase 1 Parte 2):} Cuando el BOE no publica el detalle suficiente, el crawler explora de forma acotada las webs institucionales, sus sitemaps y catálogos relacionados. La resolución usa normalización léxica y límites de profundidad configurables, incorporando además el extractor universal de widgets dinámicos HTML5 / endpoints REST y el marco agnóstico de extracción de programas de doctorado (RD 99/2011) con navegación canónica a subpáginas de líneas acreditadas y descarte de binarios.
    \item \textbf{Garantía de Rastreo Ético y Cortesía Monodominio:} La exploración de los catálogos y subpáginas de una misma universidad se ejecuta de forma estrictamente secuencial a través del cliente \texttt{RUCTDownloader}, aplicando retardos corteses (\texttt{REQUEST\_DELAY} y dispersión aleatoria \emph{Jitter}), respetando las directivas \texttt{Crawl-delay} del protocolo \texttt{robots.txt} (con caché conforme a RFC 9309 con desalojo LRU) y activando el patrón \emph{Circuit Breaker} ante incidencias continuadas de servidor para aislar el dominio conflictivo.
    \item \textbf{Arquitectura de Caché Jerárquica Dual (L1 RAM / L2 SQLite WAL):} El sistema implementa un esquema de almacenamiento intermedio en dos niveles para guías docentes y URLs candidatas. Las peticiones resueltas o descartadas (códigos 404/410) se consolidan en tablas SQLite bajo modo \emph{Write-Ahead Logging} (WAL) y se replican en diccionarios de memoria RAM protegidos por cerrojos reentrantes, permitiendo omitir enlaces no válidos en $0\text{ ms}$ y minimizando el tráfico de red.
    \item \textbf{Caché Criptográfica de Modelos de Documento (SHA-256):} Para optimizar el análisis de resoluciones del BOE que aprueban múltiples titulaciones de forma simultánea, el analizador vectorial mantiene un búfer LRU en memoria indexado por la huella digital SHA-256 del contenido del PDF, garantizando tiempo de análisis de $\approx 0.9\text{ ms}$ en lecturas secundarias.
    \item \textbf{Capa de Normalización Curricular y Detección Idiomática:} Módulo desacoplado de enriquecimiento que infiere automáticamente la lengua académica de impartición (\emph{Castellano, Català, Galego, Euskara, English}) y normaliza los criterios de calificación en un esquema porcentual estructurado (\texttt{teoria\_porcentaje}, \texttt{practicas\_porcentaje}, \texttt{evaluacion\_continua\_porcentaje}, \texttt{examen\_final\_porcentaje}).
    \item \textbf{Centralización y Parametrización en \texttt{config.py}:} Todos los parámetros y cotas del sistema de ingesta (\texttt{HUB\_AND\_SPOKE\_MAX\_HUBS}, \texttt{HUB\_AND\_SPOKE\_MAX\_DEPTH}, \texttt{HUB\_ACADEMIC\_KEYWORDS}, timeouts, reintentos y cuotas ECTS) se encuentran formalmente centralizados en el archivo de configuración con soporte para sobreescritura dinámica mediante variables de entorno del sistema operativo analizadas con convertidores protegidos contra excepciones.
    \item \texttt{routes/universidades.py}: Endpoints con ordenación prioritaria (Públicas primero) y operaciones CRUD.
    \item \texttt{routes/titulaciones.py}: Endpoints para Grados, Másteres y Doctorados, lectura de planes de estudio BOE, filtrado vocacional por Rama de Conocimiento (\texttt{rama}) y controladores CRUD completos para la gestión de asignaturas y elementos curriculares (\texttt{GET/POST /api/v1/titulaciones/\{codigo\}/asignaturas}, \texttt{PUT/DELETE /api/v1/titulaciones/asignaturas/\{id\}}).
    \item \texttt{routes/estadisticas.py}: Endpoint \texttt{/api/v1/estadisticas/cobertura} que expone métricas calculadas sobre los datos cargados. Los porcentajes de cobertura, caché o huella deben interpretarse junto con la fecha, el manifiesto y la configuración de la ejecución de origen.
    \item \texttt{metrics/container\_metrics.py}: Muestreo de métricas físicas de salud (RAM RSS MB, CPU \%) y huella de carbono ($gCO_2$) por contenedor cgroup.
\end{itemize}

\subsection{Seguridad y Control de Acceso (RBAC)}
La API REST implementa un modelo de Control de Acceso Basado en Roles (RBAC) para proteger las operaciones críticas y la integridad de los datos de producción:
\begin{itemize}
    \item \textbf{Usuarios Estándar (Lectura):} El 99\% de los consumidores (incluyendo el frontend público) interactúan a través de endpoints GET sin autenticación, respaldados en base de datos por un usuario \texttt{unihub\_api\_user} con privilegios estrictos de \texttt{GRANT SELECT}.
    \item \textbf{Administradores (Escritura):} Operaciones CRUD (Creación, Modificación, Eliminación de universidades, titulaciones y asignaturas) y ejecución de volcados de datos (ETL) requieren autenticación mediante la cabecera HTTP \texttt{X-API-Key}. La verificación utiliza \texttt{secrets.compare\_digest} en \texttt{security.py} para garantizar tiempo constante en la comparación y prevenir ataques de canal lateral (timing attacks).
    \item \textbf{Blindaje de Conexiones a Base de Datos:} En \texttt{config.py}, la construcción de la cadena de conexión SQLAlchemy empaqueta los parámetros con \texttt{urllib.parse.quote\_plus}, desinfectando caracteres especiales en contraseñas o nombres de usuario (\texttt{@}, \texttt{/}, \texttt{\%}, etc.) y previniendo inyecciones de credenciales.
\end{itemize}

\section{Parametrización del software base}
Se configuró el servidor proxy inverso Nginx (\texttt{nginx.conf}) para redirigir las peticiones \texttt{/api/v1/} hacia la API REST en \texttt{http://api:8000/api/v1/} y servir la SPA React en la raíz \texttt{/}, soportando react-router para la URL \texttt{/admin}. En PostgreSQL, se parametrizó la creación del usuario restringido \texttt{unihub\_api\_user} con permisos \texttt{GRANT SELECT} exclusivo, utilizando cadenas de conexión URL-encoded mediante \texttt{quote\_plus} en el módulo de configuración de la API (\texttt{config.py}).

\section{Diseño Físico de Datos}
El diseño relacional en PostgreSQL (\texttt{schema.sql}) se compone de las siguientes tablas principales con claves primarias y foráneas con borrado en cascada, optimizadas mediante la extensión de trigramas \texttt{pg\_trgm}:
\begin{itemize}
    \item \texttt{universidades}: Clave primaria \texttt{codigo VARCHAR(10)}. Índices en \texttt{tipo}, \texttt{comunidad\_autonoma} e índice GIN trigrama \texttt{idx\_univ\_nombre\_trgm} sobre \texttt{nombre}.
    \item \texttt{titulaciones}: Clave primaria \texttt{codigo\_estudio VARCHAR(20)}, clave foránea \texttt{universidad\_codigo REFERENCES universidades(codigo)}. Índice GIN trigrama \texttt{idx\_tit\_titulo\_trgm} sobre \texttt{titulo}.
    \item \texttt{planes\_estudio}: Clave foránea \texttt{codigo\_estudio REFERENCES titulaciones(codigo\_estudio)}. Incorpora las columnas de trazabilidad \texttt{origen\_fuente VARCHAR(100)} y \texttt{pdf\_sha256 VARCHAR(64)}, campos de calidad y verificación (\texttt{estado\_calidad}, \texttt{motivos\_calidad}, \texttt{fuente\_verificada\_url}, \texttt{verificado\_en}), tipo estructural (\texttt{tipo\_estructura}, \texttt{ects\_exigidos}) y la columna semiestructurada \texttt{programa\_doctoral JSONB} para almacenar las líneas de investigación acreditadas, escuela tutelar y actividades formativas bajo el RD 99/2011.
    \item \texttt{resumen\_creditos}: Clave foránea a \texttt{planes\_estudio(id)}.
    \item \texttt{elementos\_curriculares}: Clave foránea a \texttt{planes\_estudio(id)}. Tipos de datos en texto extenso (\texttt{TEXT}) para evitar truncamientos.
    \item \texttt{errores\_crawler} y \texttt{estadisticas\_rendimiento}: Tablas relacionales de registro de auditoría con desduplicación por timestamp.
\end{itemize}

\section{Diseño de la Interfaz de Usuario} 
El diseño visual y la experiencia de usuario (UX/UI) de la aplicación web de \textbf{UniHub} se han concebido bajo los estándares modernos de desarrollo de Single Page Applications (SPA) con React 19 y Vite, siguiendo estrictamente la identidad visual corporativa de la \textbf{Universidad de Cádiz (UCA)}:
\begin{itemize}
    \item \textbf{Azul Noche UCA (Color Primario y Navegación):} \texttt{\#002B49} y \texttt{\#003366}, empleado en barras de cabecera, tipografía de títulos y banners principales.
    \item \textbf{Azul Mar de Cádiz (Acentos y Acciones):} \texttt{\#0084C8} y \texttt{\#00A8E8}, utilizado en botones de acción principal, enlaces interactivos y bordes destacados.
    \item \textbf{Dorado Sol de Cádiz (Avisos e Insignias Especiales):} \texttt{\#F3A712} y \texttt{\#FFC72C}, aplicado en insignias de advertencia y registros gestionados por administración.
    \item \textbf{Verde Aprobado / Éxito (Insignia Verificado):} \texttt{\#10B981}, aplicado en distintivos de planes completos verificados conforme a publicación en BOE.
    \item \textbf{Rosa Magenta (Distintivo Doctorado):} \texttt{\#EC4899} (con fondo al 15\,\% de opacidad), específico para programas oficiales de doctorado e investigación (RD 99/2011).
\end{itemize}

\subsection{Vistas Principales y Prototipado del Sistema}
A continuación se describen y documentan visualmente las pantallas nucleares de la plataforma mediante capturas de la interfaz implementada.

\subsubsection{Pantalla Principal y Catálogo de Universidades}
La vista principal integra la barra de navegación superior, el banner institucional con métricas globales en tiempo real y la grilla interactiva de universidades (Figura~\ref{fig:ui_catalogo}).

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/ui_catalogo_universidades.png}
    \caption{Interfaz de usuario: Pantalla de inicio y catálogo interactivo de universidades con buscador y filtros (Fase 3)}
    \label{fig:ui_catalogo}
\end{figure}

Cada tarjeta universitaria expone su denominación oficial, tipología institucional (Pública o Privada), comunidad autónoma y el desglose de oferta académica (Grados, Másteres y Doctorados). Asimismo, incorpora controles de paginación configurable (5, 10, 20, 50 y 100 elementos por página) con desplazamiento suave automático al inicio tras cada transición.

\subsubsection{Modal de Visualización Curricular y Menciones Oficiales}
Al pulsar sobre el botón de consulta de una titulación, el sistema despliega un diálogo modal flotante con desenfoque de fondo (\textit{glassmorphism}) que renderiza la estructura docente (Figura~\ref{fig:ui_modal_plan}).

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/ui_modal_plan_estudios.png}
    \caption{Interfaz de usuario: Modal de detalle curricular con asignaturas, créditos ECTS, cuatrimestres y trazabilidad BOE}
    \label{fig:ui_modal_plan}
\end{figure}

El modal incluye:
\begin{enumerate}
    \item \textbf{Encabezado de Calidad y Trazabilidad:} Muestra la insignia de calidad del plan (\emph{Verificado BOE, Universidad o Administración}) y proporciona el botón directo hacia la disposición legal original.
    \item \textbf{Desglose Curricular por Cursos y Cuatrimestres:} Agrupa las asignaturas estructuradas (1\textordmasculine{} a 4\textordmasculine{} curso; 1C, 2C y Anual) con indicación de tipología (Básica, Obligatoria, Optativa) y carga en créditos ECTS.
    \item \textbf{Menciones Curriculares e Itinerarios:} Detección y filtrado visual de itinerarios formativos oficiales aprobados en el plan de estudios.
    \item \textbf{Tratamiento Específico de Doctorados:} Para programas de doctorado, sustituye la tabla de asignaturas por la tarjeta de investigación con líneas acreditadas, escuela tutelar y régimen de tutela académica anual.
\end{enumerate}

\subsubsection{Simulador Financiero de Matrícula Universitaria}
La herramienta de liquidación de matrícula (\texttt{TuitionCalculator}) permite al estudiante estimar con exactitud el coste económico de sus estudios en centros públicos y privados (Figura~\ref{fig:ui_calculadora}).

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/ui_calculadora_matricula.png}
    \caption{Interfaz de usuario: Estimador interactivo de liquidación de matrícula con repeticiones y exenciones sociales}
    \label{fig:ui_calculadora}
\end{figure}

El simulador implementa en tiempo real:
\begin{itemize}
    \item Selección granular de asignaturas individuales o cursos completos.
    \item Modificación del grado de repetición (1\textordfeminine{}, 2\textordfeminine{}, 3\textordfeminine{} o 4\textordfeminine{}+ matrícula) aplicando los coeficientes multiplicadores autonómicos.
    \item Aplicación de exenciones sociales (Familia Numerosa General y Especial, Discapacidad $\ge 33\,\%$, Beca MEC del Ministerio, Matrícula de Honor y Bonificación del 99\,\% de la Junta de Andalucía).
    \item Generación instantánea del recibo de liquidación con desglose de créditos, tasas de secretaría y bonificación total.
\end{itemize}

\subsubsection{Búsqueda por Proximidad Geográfica (Fórmula de Haversine)}
La vista de geolocalización permite a los usuarios ordenar el catálogo de centros universitarios en función de su distancia física real en kilómetros (Figura~\ref{fig:ui_geolocalizacion}).

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/ui_geolocalizacion.png}
    \caption{Interfaz de usuario: Búsqueda y ordenación de universidades por proximidad geográfica mediante geolocalización}
    \label{fig:ui_geolocalizacion}
\end{figure}

El cálculo se ejecuta en el cliente mediante la fórmula trigonométrica del semiverseno (Haversine), tomando como referencia las coordenadas GPS del navegador del usuario o una capital de provincia seleccionada manualmente entre las 52 provincias españolas.

\subsection{Panel de Control y Administración Aislado}
Ubicado bajo la ruta de acceso manual \texttt{/admin} y protegido mediante autenticación por clave API (\texttt{X-API-Key}), el panel de administración centraliza la monitorización y gestión de la plataforma:
\begin{enumerate}
    \item \textbf{Herramientas de Exportación Masiva CRUD:} Descarga en formatos CSV (con codificación \texttt{UTF-8 BOM} para compatibilidad total con hojas de cálculo) y JSON estructurado, sanitizando caracteres iniciales potencialmente peligrosos (\texttt{=}, \texttt{+}, \texttt{-}, \texttt{@}) para neutralizar ataques de CSV Injection.
    \item \textbf{Gestor Modal de Asignaturas y Precios ECTS:} Subpanel para añadir, editar o dar de baja materias curriculares, bloqueando modificaciones accidentales sobre planes verificados mediante la marca \texttt{gestionado\_por\_admin}.
    \item \textbf{Semáforos Core Web Vitals y Latencia de Base de Datos:} Indicadores de rendimiento en vivo (TTFB, FCP y LCP) conforme a las directrices de Google CWV, complementados con la medición de latencia en milisegundos de la conexión PostgreSQL.
    \item \textbf{Telemetría Docker y Sostenibilidad Green IT:} Monitorización continua de consumo de memoria RAM RSS y porcentaje de uso de CPU por contenedor, complementado con la estimación de emisiones de carbono ($g\text{CO}_2$).
    \item \textbf{Monitor de Sincronización ETL en Vivo:} Banner reactivo que notifica el progreso de la carga de datos masiva y la liberación de cerrojos de proceso (\texttt{etl\_running.lock}).
\end{enumerate}
